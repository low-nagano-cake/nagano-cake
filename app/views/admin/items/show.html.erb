商品の詳細ページ

<table class="table table-striped">
   <thead></thead>
	<tbody>
		<tr>
			<td><%= image_tag @item.image, size: "100x100" %></td>
			<td>商品名<%= @item.name %></td>
			<td>商品説明<%= @item.introduction %></td>
			<td>ジャンル<%= @item.genre.name %></td>
			<td>税込価格(税抜価格)<%= @item.add_tax_price.to_s(:delimited) %>(<%= @item.price %>)円
			<td><% if @item.is_active == true %>販売中
			    <% else @item.is_active == false %>販売停止中</td>
			    <% end %>
			<td><%= link_to "編集", edit_admin_item_path(@item.id) %></td>
	  </tr>
	</tbody>
</table>


